{
  "name": "ugs-zip-upload-function",
  "version": "1.0.0",
  "description": "Cloud Function to upload UGS dataset zips to GCS",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/storage": "^7.7.0"
  },
  "scripts": {
    "start": "functions-framework --target=uploadZip",
    "deploy": "gcloud functions deploy ugs-zip-upload --runtime nodejs18 --trigger-http --allow-unauthenticated --region us-central1 --memory 512MB --timeout 540s"
  }
}